import styled from "@emotion/styled";

const FormConatainer = styled("div")(()=>({
    display:"flex",
    flexDirection:"column",
    justifyContent:"space-between",
    alignItems:"center",
    gap:40,
    width:"100%",
  }))

export default FormConatainer